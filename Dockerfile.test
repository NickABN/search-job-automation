FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
COPY alembic.ini ./
COPY alembic ./alembic
COPY tests ./tests
RUN pip install --no-cache-dir ".[dev]"

CMD ["sh", "-c", "alembic upgrade head && python -m pytest -m integration -q"]
